# Schema asks (raised by playground designs)

Anything I want to design with that the real schema doesn't have today.
Each entry will eventually become its own additive-only schema PR per
`~/git/thea/.claude/rules/schemas.md` — additive only, tagged to Manny,
landed before any consumer-side PR.

## Format

```
### <field name>
- **Where:** <which schema file at thea-shared-schemas/schemas/*.json>
- **Type:** <optional addition only — never required, never narrowed>
- **Why:** <design need>
- **Mock value used:** <what the playground stub returns>
- **Status:** open | proposed (PR #X) | landed
```

## Open asks

_(none yet — added as design ideas surface)_
